name: Encoding Guard

on:
  push:
  pull_request:

jobs:
  lint-encoding:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run encoding/mojibake scanner
        shell: pwsh
        run: |
          ./scripts/encoding-scan.ps1